export function validateEmail(value: string) {
    if (!value) return "Ем… ти нічого не ввів";

    if (/\s/.test(value)) return "Оу! У пошті не можна пробіли";

    const parts = value.split("@");
    if (parts.length < 2) return "Трішки бракує символу '@'";
    if (parts.length > 2) return "Ага… у тебе аж два '@', так не можна";

    const [local, domain] = parts;

    if (!local.length) return "Перед '@' має щось бути";
    if (!/^[a-zA-Z0-9._%+-]+$/.test(local))
        return "Є дивні символи перед '@'";

    if (!domain.length) return "Після '@' має бути домен, а його немає";

    const domainParts = domain.split(".");
    if (domainParts.length < 2)
        return "У домені має бути крапочка. Хоч одна. Будь ласка.";

    const tld = domainParts[domainParts.length - 1];

    if (tld.length < 2)
        return "Кінцівка домену закоротка (має бути хоча б 2 символи)";

    if (!/^[a-zA-Z0-9.-]+$/.test(domain))
        return "У домені є підозрілі символи";

    if (domain.startsWith(".") || domain.endsWith("."))
        return "Крапка не може стояти з краю";

    return null; // все ок 🍀
}

export function validatePassword(value: string) {
    if (!value) return "Ти ще нічого не ввів";

    // мінімальна довжина, але без нервів
    if (value.length < 4)
        return "Хмм… пароль трохи коротенький (зроби хоча б 4 символів)";

    // підказка, не вимога
    // if (!/[A-Z]/.test(value))
    //     return "Можеш додати одну велику літеру — буде трішки надійніше 😉";

    // // підказка, не вимога
    // if (!/[0-9]/.test(value))
    //     return "Додай цифру — виглядатиме солідніше 😎";

    // // підказка, не вимога
    // if (!/[!@#$%^&*(),.?":{}|<>]/.test(value))
    //     return "Маленький символ типу ! або @ додасть +10 захисту 🛡️✨";

    return null; // все ок 🌟
}
